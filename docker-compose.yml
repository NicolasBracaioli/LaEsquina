version: "3.9"

services:
  laesquina:
    build:
      context: .
      dockerfile: Dockerfile.backend   # asegurate que tu archivo se llame así
    container_name: laesquina_app
    ports:
      - "1573:5173"   # afuera 1573 → adentro Vite usa 5173
      - "3000:3000"   # backend expuesto en 3000
    environment:
      DB_HOST: 192.168.150.61   # 👈 IP de tu host donde corre el contenedor MySQL
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: Admin123
      DB_NAME: testdb
    networks:
      - default
