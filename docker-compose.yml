version: "3.9"
services:
  laesquina:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: laesquina_app
    ports:
      - "3000:3000"   # API y frontend por el mismo puerto
    environment:
      - DB_HOST=192.168.150.61   # donde corre tu MySQL
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=Admin123
      - DB_NAME=testdb
