version: "3.9"

services:
  laesquina:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: laesquina_app
    ports:
      - "1573:5173"   # frontend afuera en 1573
      - "3000:3000"   # backend
    environment:
      DB_HOST: 192.168.150.61   # IP del host / contenedor MySQL
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: Admin123
      DB_NAME: testdb
